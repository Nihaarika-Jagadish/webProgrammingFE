import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#1d3557",
    },
    secondary: {
      main: "#FFFFFF",
    },
    text:{
      primary: "#000000"
    },
    warning:{
      main: "#000000"
    },
    success:{
      main: "#000000"
    }
  },
});

export default theme;